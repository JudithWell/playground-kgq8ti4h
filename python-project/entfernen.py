einkaufsliste = ["Mehl", "Backpulver", "Zucker", "Margarine", "Eier"]
mengen = [400, 1, 250, 200, 4]

# Entfernt das Element an der Stelle index aus beiden Listen.
def entfernenIndex(index):
    einkaufsliste.pop(index)
    mengen.pop(index)

def entfernen(produkt):
    # Ergänze hier deinen Code:

